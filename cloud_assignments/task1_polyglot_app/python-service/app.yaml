runtime: python311
service: python-service
instance_class: F1
env_variables:
  MONGODB_URI: "<set-in-app-engine-or-secret-manager>"
  DB_NAME: "cloud_db"

entrypoint: uvicorn main:app --host 0.0.0.0 --port 8080
